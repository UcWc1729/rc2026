# 自动串口发送机制分析

## ✅ 代码可以实现自动串口发送

### 数据流链路

```
键盘控制节点 (rc_keyboard_control)
    ↓
定时器 (20Hz) → update_and_publish()
    ↓
发布 geometry_msgs/Twist 到 /cmd_vel 话题
    ↓
串口驱动节点 (rc_serial_driver)
    ↓
订阅回调 cmdVelCallback() 自动触发
    ↓
sendChassisSpeedCommand() 打包协议
    ↓
serial_port_.write() 自动发送到串口
    ↓
STM32下位机
```

## 详细分析

### 1. 键盘控制节点 - 自动发布机制

**文件**: `src/rc_keyboard_control/rc_keyboard_control/keyboard_control_node.py`

```python
# 第58-59行：创建定时器，定期发布
timer_period = 1.0 / publish_rate  # 默认 1/20 = 0.05秒 (20Hz)
self.timer = self.create_timer(timer_period, self.update_and_publish)

# 第155-190行：定时回调函数
def update_and_publish(self):
    # 根据按下的按键更新速度
    # ...
    # 发布速度指令（无论速度是否为0都会发布）
    self.cmd_vel_pub.publish(self.current_twist)
```

**特点**:
- ✅ 定时器每 0.05秒（20Hz）自动调用 `update_and_publish()`
- ✅ 无论是否有按键按下，都会定期发布消息
- ✅ 如果按键按下，发布非零速度；如果没按键，发布零速度

### 2. 串口驱动节点 - 自动接收和发送机制

**文件**: `src/rc_serial_driver/src/serial_driver_node.cpp`

```cpp
// 第51-55行：创建订阅者
cmd_vel_sub_ = this->create_subscription<geometry_msgs::msg::Twist>(
  cmd_vel_topic,
  10,  // 队列大小
  std::bind(&SerialDriverNode::cmdVelCallback, this, std::placeholders::_1)
);

// 第86-97行：订阅回调函数（自动触发）
void cmdVelCallback(const geometry_msgs::msg::Twist::SharedPtr msg)
{
  last_cmd_time_ = this->now();
  
  // 限制速度范围
  double vx = std::max(-max_linear_vel_, std::min(msg->linear.x, max_linear_vel_));
  double vy = std::max(-max_linear_vel_, std::min(msg->linear.y, max_linear_vel_));
  double vtheta = std::max(-max_angular_vel_, std::min(msg->angular.z, max_angular_vel_));
  
  // 自动发送速度指令
  sendChassisSpeedCommand(vx, vy, vtheta);
}

// 第109-154行：协议打包和串口发送
void sendChassisSpeedCommand(double vx, double vy, double vtheta)
{
  // 1. 打包数据（vx, vy, wz）
  uint8_t data[12];
  float vx_f = static_cast<float>(vx);
  float vy_f = static_cast<float>(vy);
  float wz_f = static_cast<float>(vtheta);
  memcpy(&data[0], &vx_f, 4);
  memcpy(&data[4], &vy_f, 4);
  memcpy(&data[8], &wz_f, 4);
  
  // 2. 打包协议帧
  uint16_t frame_len = packFrame(
    CmdID::CHASSIS_SPEED,  // 0x0101
    data, 12, tx_seq_, frame_buffer
  );
  
  // 3. 自动发送到串口
  int bytes_sent = serial_port_.write(frame_buffer, frame_len);
  
  // 4. 序列号自增
  tx_seq_ = (tx_seq_ + 1) % 256;
}
```

**特点**:
- ✅ ROS2订阅机制：每当 `/cmd_vel` 话题有新消息时，`cmdVelCallback` 自动触发
- ✅ 无需手动调用：完全由ROS2消息机制自动驱动
- ✅ 实时响应：消息到达后立即处理并发送
- ✅ 协议完整：包含SOF、LEN、SEQ、CRC8、CMD_ID、DATA、CRC16

### 3. 超时保护机制

```cpp
// 第63-66行：超时定时器
timeout_timer_ = this->create_wall_timer(
  std::chrono::milliseconds(static_cast<int>(timeout_ * 1000)),  // 默认100ms
  std::bind(&SerialDriverNode::timeoutCallback, this)
);

// 第99-107行：超时检测
void timeoutCallback()
{
  auto elapsed = (this->now() - last_cmd_time_).seconds();
  if (elapsed > timeout_) {  // 超过100ms未收到新消息
    sendStopCommand();  // 自动发送停止指令
  }
}
```

**特点**:
- ✅ 安全保护：如果100ms内未收到新消息，自动发送停止指令
- ✅ 防止失控：确保下位机不会因为通信中断而继续运动

## 发送频率分析

### 当前配置

| 组件 | 频率 | 说明 |
|------|------|------|
| 键盘控制发布 | 20 Hz | 每50ms发布一次 |
| 串口驱动发送 | 20 Hz | 每50ms发送一次（跟随发布频率） |
| 超时检测 | 10 Hz | 每100ms检查一次 |

### 实际发送情况

1. **有按键按下时**:
   - 键盘控制节点每50ms发布一次非零速度
   - 串口驱动每50ms自动接收并发送一次
   - **发送频率**: 20 Hz

2. **无按键按下时**:
   - 键盘控制节点每50ms发布一次零速度（停止）
   - 串口驱动每50ms自动接收并发送一次停止指令
   - **发送频率**: 20 Hz

3. **通信中断时**:
   - 如果100ms内未收到新消息
   - 超时定时器自动触发，发送停止指令
   - **安全保护**: 确保下位机停止

## 协议帧格式验证

每次发送的完整帧结构：

```
SOF (0xA5)          1字节
LEN (0x000E)        2字节  (2字节CMD_ID + 12字节DATA)
SEQ (0x00-0xFF)     1字节  (自动递增，0-255循环)
CRC8                1字节  (帧头校验)
CMD_ID (0x0101)     2字节  (底盘速度控制)
DATA (12字节)       12字节  (vx, vy, wz各4字节)
CRC16               2字节  (整帧校验)
----------------------------------------
总计: 21字节/帧
```

## 结论

### ✅ 代码完全支持自动串口发送

1. **自动发布**: 键盘控制节点通过定时器自动发布消息（20Hz）
2. **自动接收**: 串口驱动节点通过ROS2订阅机制自动接收消息
3. **自动发送**: 收到消息后自动打包协议并发送到串口
4. **自动保护**: 超时机制确保通信中断时自动停止

### 工作流程

```
启动键盘控制节点
    ↓
定时器每50ms触发
    ↓
update_and_publish() 自动执行
    ↓
发布消息到 /cmd_vel
    ↓
串口驱动节点自动接收（ROS2订阅机制）
    ↓
cmdVelCallback() 自动触发
    ↓
sendChassisSpeedCommand() 自动执行
    ↓
协议打包（packFrame）
    ↓
serial_port_.write() 自动发送
    ↓
数据到达STM32下位机
```

### 无需手动操作

- ❌ 不需要手动调用发送函数
- ❌ 不需要手动触发回调
- ❌ 不需要手动管理定时器
- ✅ 完全自动化，由ROS2消息机制驱动

## 验证方法

### 1. 查看话题数据流

```bash
# 查看 /cmd_vel 话题发布频率
ros2 topic hz /cmd_vel

# 查看消息内容
ros2 topic echo /cmd_vel
```

### 2. 查看串口数据

```bash
# 使用串口调试工具查看实际发送的数据
# 应该能看到每50ms发送一次协议帧
```

### 3. 查看节点日志

```bash
# 串口驱动节点会输出发送日志
ros2 run rc_serial_driver serial_driver_node
```

## 优化建议（可选）

如果需要降低发送频率以节省带宽，可以：

1. **降低键盘控制发布频率**:
   ```python
   # 修改 publish_rate 参数
   -p publish_rate:=10.0  # 改为10Hz
   ```

2. **串口驱动会自动跟随**:
   - 发布频率降低，发送频率也会自动降低
   - 超时时间可以相应调整
