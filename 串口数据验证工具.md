# 串口数据验证工具

## 数据流验证

### 1. 键盘控制 → ROS2消息

**键盘控制节点** (`keyboard_control_node.py`):
```python
W键 → twist.linear.x = 0.2  (前进)
S键 → twist.linear.x = -0.2 (后退)
A键 → twist.linear.y = 0.2  (左移)
D键 → twist.linear.y = -0.2 (右移)
Q键 → twist.angular.z = 0.2  (逆时针)
E键 → twist.angular.z = -0.2 (顺时针)
```

### 2. ROS2消息 → 串口驱动

**串口驱动节点** (`serial_driver_node.cpp`):
```cpp
vx = msg->linear.x      // 前进速度
vy = msg->linear.y      // 横向速度
vtheta = msg->angular.z // 旋转角速度
```

### 3. 串口驱动 → 协议打包

**协议格式** (`sendChassisSpeedCommand`):
```cpp
float vx_f = vx;        // 字节 0-3
float vy_f = vy;        // 字节 4-7
float wz_f = vtheta;    // 字节 8-11
```

## 验证方法

### 方法1: 查看ROS2话题数据

```bash
# 查看 /cmd_vel 话题数据
ros2 topic echo /cmd_vel

# 按W键，应该看到：
# linear:
#   x: 0.2
#   y: 0.0
#   z: 0.0
# angular:
#   x: 0.0
#   y: 0.0
#   z: 0.0

# 按A键，应该看到：
# linear:
#   x: 0.0
#   y: 0.2
#   z: 0.0
```

### 方法2: 使用串口调试工具

```bash
# 安装串口调试工具
sudo apt-get install minicom

# 查看串口数据（十六进制）
sudo minicom -D /dev/ttyUSB0 -b 115200 -H

# 或者使用hexdump
cat /dev/ttyUSB0 | hexdump -C
```

### 方法3: 添加调试日志

在串口驱动中添加日志，打印发送的数据值。

## 可能的问题

### 问题1: 数据方向错误

**症状**: 按W键机器人后退，按A键机器人右移

**原因**: 下位机坐标系与ROS2坐标系不一致

**解决**: 在下位机端调整符号，或在上位机端取反

### 问题2: 数据值错误

**症状**: 速度不对，或者没有反应

**原因**: 
- 速度单位不匹配（m/s vs cm/s）
- 速度范围限制

**检查**: 
- 查看串口驱动的 `max_linear_vel` 和 `max_angular_vel` 参数
- 检查下位机期望的速度单位

### 问题3: 协议格式错误

**症状**: 下位机完全没反应

**原因**: 
- 协议帧格式错误
- CRC校验失败
- 命令ID错误

**检查**: 
- 使用串口调试工具查看实际发送的数据
- 对比协议文档检查帧格式

## 调试步骤

1. **验证ROS2消息**: `ros2 topic echo /cmd_vel`
2. **检查串口驱动日志**: 查看是否有错误或警告
3. **使用串口调试工具**: 查看实际发送的原始数据
4. **对比协议文档**: 检查数据格式是否正确
