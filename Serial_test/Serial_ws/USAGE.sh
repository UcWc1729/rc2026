#!/bin/bash

cat << 'EOF'

╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║          🚀 底盘速度控制系统 - 使用指南                       ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

📋 功能说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ 通过串口(115200)发送底盘速度控制命令
  ✓ 支持全向移动: Vx(前进), Vy(横移), Wz(旋转)
  ✓ 完整的通信协议实现(CRC8/CRC16双重校验)
  ✓ 自动发送心跳包保持连接
  ✓ 启动时自动发送测试命令: Vx=0.1, Vy=0, Wz=0

📦 已生成的数据帧
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  帧数据: a5 0e00 00 bc 0101 cdcccc3d 00000000 00000000 7278
  
  解析:
    A5        - 帧头标志
    0E00      - 数据长度(14字节)
    00        - 序列号
    BC        - CRC8校验
    0101      - 命令ID(底盘速度控制)
    CDCCCC3D  - Vx = 0.1 m/s
    00000000  - Vy = 0.0 m/s  
    00000000  - Wz = 0.0 rad/s
    7278      - CRC16校验
  
  总长度: 21字节 ✓

🎯 快速测试 (3种方式)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

方式1: 一键测试(推荐) 🌟
────────────────────────────────────────────────────────────
  ./quick_test.sh
  
  功能: 
    • 自动检查串口设备
    • 自动添加权限
    • 发送测试命令
    • 显示详细信息

方式2: ROS2节点
────────────────────────────────────────────────────────────
  # 启动节点(会自动发送测试命令)
  ./start_chassis_controller.sh
  
  # 或手动启动
  source /opt/ros/humble/setup.bash
  source install/setup.bash
  ros2 run my_package_py chassis_controller

  # 在另一终端通过话题发送速度
  ros2 topic pub /cmd_vel geometry_msgs/msg/Twist \
    "{linear: {x: 0.5, y: 0.0, z: 0.0}}"

方式3: Python脚本
────────────────────────────────────────────────────────────
  python3 test_chassis_speed.py

📊 参数说明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  串口参数:
    设备:   /dev/ttyUSB0
    波特率: 115200
    数据位: 8
    停止位: 1
    校验:   None

  速度范围:
    Vx: -5.0 ~ 5.0 m/s   (前进/后退)
    Vy: -5.0 ~ 5.0 m/s   (左移/右移, 全向轮)
    Wz: -3.14 ~ 3.14 rad/s (左转/右转)

🔧 修改速度值
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. 修改测试脚本中的速度:
     编辑 test_chassis_speed.py 第24行:
     vx, vy, wz = 0.5, 0.2, 0.3  # 修改为你想要的值

  2. 通过ROS2话题发送:
     ros2 topic pub /cmd_vel geometry_msgs/msg/Twist \
       "{linear: {x: 0.5, y: 0.2, z: 0.0}, angular: {z: 0.3}}"

  3. 修改节点默认值:
     编辑 chassis_speed_controller.py 第109行

🐛 故障排除
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  问题1: 串口权限错误
  解决: sudo chmod 666 /dev/ttyUSB0
       或: sudo usermod -aG dialout $USER (需重新登录)

  问题2: 找不到串口设备
  解决: ls /dev/ttyUSB* /dev/ttyACM*
       检查USB线是否连接

  问题3: 串口被占用
  解决: lsof /dev/ttyUSB0
       关闭占用的程序

📁 文件清单
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  核心代码:
    ✓ src/my_package_py/my_package_py/protocol_handler.py
    ✓ src/my_package_py/my_package_py/chassis_speed_controller.py
  
  测试工具:
    ✓ test_chassis_speed.py          (Python测试脚本)
    ✓ quick_test.sh                  (一键测试)
    ✓ start_chassis_controller.sh    (ROS2启动脚本)
  
  文档:
    ✓ 实现总结.md                    (成功报告)
    ✓ CHASSIS_CONTROL_README.md      (详细文档)
    ✓ USAGE.sh                       (本文件)

📚 协议文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  参考: 通用上下位机通信协议设计文档.md
  C代码: protocol_chassis.c / protocol_chassis.h

🎯 下位机集成
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  1. 将 protocol_chassis.c 和 .h 添加到STM32项目
  2. 在main.c中初始化协议: Protocol_Init()
  3. 注册回调函数处理速度命令
  4. 在UART接收中断调用: Protocol_ProcessData()

✨ 测试成功标志
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✓ 生成21字节数据帧
  ✓ CRC校验通过
  ✓ 串口发送成功
  ✓ ROS2节点正常运行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      🎉 实现完成! 🎉
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EOF
