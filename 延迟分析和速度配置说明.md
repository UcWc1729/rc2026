# 延迟分析和速度配置说明

## 延迟原因分析

### 1. 键盘控制节点发布频率（主要延迟源）

**当前配置**: 20Hz (每50ms发布一次)

```python
# keyboard_control_node.py 第58行
timer_period = 1.0 / publish_rate  # 默认 1/20 = 0.05秒 = 50ms
```

**延迟**: 最多50ms（按键按下到消息发布的延迟）

### 2. ROS2消息传递延迟

- 话题订阅/发布机制：通常 < 5ms
- 消息序列化/反序列化：通常 < 2ms

**总延迟**: 约 5-10ms

### 3. 串口驱动处理延迟

- 回调函数处理：< 1ms
- 协议打包：< 1ms

**总延迟**: 约 1-2ms

### 4. 串口传输延迟

**波特率**: 115200 bps

**单帧数据**: 21字节（协议帧）
- 传输时间 = (21字节 × 10位/字节) / 115200 = 0.18ms

**总延迟**: 约 0.2-0.5ms（包含串口缓冲）

### 5. 下位机处理延迟

取决于STM32的处理速度，通常 < 5ms

## 总延迟估算

```
键盘控制延迟:     0-50ms  (平均25ms)
ROS2消息传递:     5-10ms
串口驱动处理:     1-2ms
串口传输:         0.2-0.5ms
下位机处理:       5ms
-----------------------------------
总延迟:           约 35-70ms (平均约50ms)
```

## 延迟优化方案

### 方案1: 提高键盘控制发布频率（推荐）

**当前**: 20Hz (50ms)
**优化**: 50Hz (20ms) 或 100Hz (10ms)

```python
# 修改 keyboard_control_node.py
self.declare_parameter('publish_rate', 50.0)  # 改为50Hz
```

**效果**: 延迟从平均50ms降低到平均20ms

### 方案2: 提高串口波特率（如果下位机支持）

**当前**: 115200 bps
**优化**: 921600 bps 或 460800 bps

```yaml
# serial_driver_params.yaml
baudrate: 921600  # 改为921600
```

**效果**: 串口传输延迟从0.5ms降低到0.06ms（但影响很小）

### 方案3: 使用更快的消息传递

- 使用ROS2的实时配置
- 减少消息队列大小
- 使用零拷贝（Zero-Copy）

## 速度配置方法

### 方法1: 修改键盘控制节点默认速度（已修改）

**文件**: `src/rc_keyboard_control/rc_keyboard_control/keyboard_control_node.py`

```python
# 第38-39行
self.declare_parameter('linear_speed', 0.2)      # 线速度 (m/s)
self.declare_parameter('angular_speed', 0.2)     # 角速度 (rad/s)
```

**当前值**: 0.2 m/s 和 0.2 rad/s

### 方法2: 运行时通过参数修改

```bash
# 启动时指定速度
ros2 run rc_keyboard_control keyboard_control_node \
    --ros-args \
    -p linear_speed:=0.5 \
    -p angular_speed:=0.5
```

### 方法3: 修改串口驱动的最大速度限制

**文件**: `src/rc_serial_driver/config/serial_driver_params.yaml`

```yaml
max_linear_vel: 1.0    # 最大线速度 (m/s)
max_angular_vel: 1.0   # 最大角速度 (rad/s)
```

**注意**: 键盘控制的速度会被这个值限制！

### 方法4: 通过launch文件修改

```bash
ros2 launch rc_serial_driver serial_driver.launch.py \
    max_linear_vel:=2.0 \
    max_angular_vel:=2.0
```

## 推荐配置

### 低延迟配置（适合快速响应）

```python
# keyboard_control_node.py
publish_rate: 50.0      # 50Hz (20ms)
linear_speed: 0.5       # 0.5 m/s
angular_speed: 0.5      # 0.5 rad/s
```

```yaml
# serial_driver_params.yaml
max_linear_vel: 2.0     # 允许更高的速度
max_angular_vel: 2.0
baudrate: 921600        # 如果下位机支持
```

### 平衡配置（推荐）

```python
# keyboard_control_node.py
publish_rate: 30.0      # 30Hz (33ms)
linear_speed: 0.3       # 0.3 m/s
angular_speed: 0.3      # 0.3 rad/s
```

## 实际延迟测试

可以使用以下命令测试延迟：

```bash
# 查看话题发布频率
ros2 topic hz /cmd_vel

# 查看消息时间戳
ros2 topic echo /cmd_vel --once
```
