# 串口数据验证指南

## 数据流检查清单

### 1. 检查ROS2话题数据

```bash
# 终端1: 查看 /cmd_vel 话题
ros2 topic echo /cmd_vel

# 按W键，应该看到：
# linear:
#   x: 0.2
#   y: 0.0
#   z: 0.0
# angular:
#   x: 0.0
#   y: 0.0
#   z: 0.0

# 按A键，应该看到：
# linear:
#   x: 0.0
#   y: 0.2    ← 全向轮：左移
#   z: 0.0
# angular:
#   z: 0.0

# 按Q键，应该看到：
# angular:
#   z: 0.2    ← 逆时针旋转
```

### 2. 检查串口驱动日志

```bash
# 查看串口驱动节点的日志
# 应该能看到：
# [DEBUG] Received cmd_vel: linear.x=0.200, linear.y=0.000, angular.z=0.000
# [DEBUG] Sending: vx=0.200 m/s, vy=0.000 m/s, wz=0.000 rad/s
```

### 3. 检查串口实际发送的数据

#### 方法1: 使用hexdump（需要另一个串口或USB转串口）

```bash
# 如果有两个串口，可以连接一个监听
cat /dev/ttyUSB1 | hexdump -C
```

#### 方法2: 使用strace跟踪系统调用

```bash
# 查看串口驱动的write调用
sudo strace -p $(pgrep -f serial_driver_node) -e write 2>&1 | grep -A 1 "write"
```

#### 方法3: 添加串口数据打印（临时调试）

可以在 `serial_port.cpp` 的 `write()` 函数中添加打印。

## 数据映射验证

### 键盘 → ROS2消息 → 串口协议

| 按键 | ROS2消息 | 串口协议 | 预期值 |
|------|----------|----------|--------|
| **W** | `linear.x = 0.2` | `vx = 0.2` | 前进 |
| **S** | `linear.x = -0.2` | `vx = -0.2` | 后退 |
| **A** | `linear.y = 0.2` | `vy = 0.2` | 左移 |
| **D** | `linear.y = -0.2` | `vy = -0.2` | 右移 |
| **Q** | `angular.z = 0.2` | `wz = 0.2` | 逆时针 |
| **E** | `angular.z = -0.2` | `wz = -0.2` | 顺时针 |
| **空格** | 全部为0 | `vx=0, vy=0, wz=0` | 停止 |

## 协议帧格式验证

### 完整帧结构（21字节）

```
字节位置 | 内容     | 值        | 说明
---------|---------|-----------|----------
0        | SOF     | 0xA5      | 帧头
1-2      | LEN     | 0x000E    | 数据长度 (14 = 2+12)
3        | SEQ     | 0x00-0xFF | 序列号
4        | CRC8    | 0xXX      | 帧头校验
5-6      | CMD_ID  | 0x0101    | 命令ID（小端序：0x01 0x01）
7-10     | vx      | float     | 前进速度 (4字节，小端序)
11-14    | vy      | float     | 横向速度 (4字节，小端序)
15-18    | wz      | float     | 旋转角速度 (4字节，小端序)
19-20    | CRC16   | 0xXXXX    | 整帧校验（小端序）
```

### 示例：按W键（前进0.2 m/s）

```
SOF:     0xA5
LEN:     0x0E 0x00  (14字节，小端序)
SEQ:     0x01
CRC8:    0xXX
CMD_ID:  0x01 0x01  (0x0101，小端序)
vx:      0xCD 0xCC 0x4C 0x3E  (0.2的float表示，小端序)
vy:      0x00 0x00 0x00 0x00  (0.0)
wz:      0x00 0x00 0x00 0x00  (0.0)
CRC16:   0xXX 0xXX  (小端序)
```

## 常见问题排查

### 问题1: 数据方向相反

**症状**: 按W键机器人后退，按A键机器人右移

**可能原因**: 下位机坐标系定义与ROS2相反

**解决方法**: 
- 在下位机端调整符号
- 或在上位机端取反（修改 `sendChassisSpeedCommand`）

### 问题2: 数据值不对

**症状**: 速度太快或太慢

**可能原因**: 
- 单位不匹配（下位机期望cm/s，但发送的是m/s）
- 速度限制参数设置不对

**检查**: 
- 查看 `max_linear_vel` 和 `max_angular_vel` 参数
- 检查下位机期望的速度单位

### 问题3: 完全没有反应

**可能原因**: 
- 协议格式错误
- CRC校验失败
- 命令ID错误
- 串口连接问题

**检查步骤**:
1. 检查串口是否打开：`ls -l /dev/ttyUSB*`
2. 检查串口驱动日志是否有错误
3. 使用串口调试工具查看实际发送的数据
4. 检查下位机是否收到数据（如果有反馈）

## 调试命令

```bash
# 1. 查看话题数据
ros2 topic echo /cmd_vel

# 2. 查看话题发布频率
ros2 topic hz /cmd_vel

# 3. 查看节点列表
ros2 node list

# 4. 查看话题连接
ros2 topic info /cmd_vel

# 5. 查看串口驱动日志（设置日志级别为DEBUG）
ros2 run rc_serial_driver serial_driver_node --ros-args --log-level debug
```
